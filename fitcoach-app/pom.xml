<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.lanprojects</groupId>
        <artifactId>fitcoach-server</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>fitcoach-app</artifactId>
    <name>FitCoach App</name>
    <description>应用启动模块：Spring Boot 入口、配置、打包</description>

    <dependencies>
        <!-- 项目内部 -->
        <dependency>
            <groupId>com.lanprojects</groupId>
            <artifactId>fitcoach-common</artifactId>
        </dependency>
        <dependency>
            <groupId>com.lanprojects</groupId>
            <artifactId>fitcoach-login</artifactId>
        </dependency>
        <dependency>
            <groupId>com.lanprojects</groupId>
            <artifactId>fitcoach-feedback</artifactId>
        </dependency>
        <dependency>
            <groupId>com.lanprojects</groupId>
            <artifactId>fitcoach-log</artifactId>
        </dependency>
        <dependency>
            <groupId>com.lanprojects</groupId>
            <artifactId>fitcoach-clientbus</artifactId>
        </dependency>
        <dependency>
            <groupId>com.lanprojects</groupId>
            <artifactId>fitcoach-exercise</artifactId>
        </dependency>
        <dependency>
            <groupId>com.lanprojects</groupId>
            <artifactId>fitcoach-membership</artifactId>
        </dependency>
        <dependency>
            <groupId>com.lanprojects</groupId>
            <artifactId>fitcoach-payment</artifactId>
        </dependency>
        <dependency>
            <groupId>com.lanprojects</groupId>
            <artifactId>fitcoach-appversion</artifactId>
        </dependency>
        <dependency>
            <groupId>com.lanprojects</groupId>
            <artifactId>fitcoach-admin</artifactId>
        </dependency>

        <!-- MySQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Spring Boot Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
