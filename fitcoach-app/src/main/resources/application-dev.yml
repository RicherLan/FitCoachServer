# ====== 开发环境配置 ======
# MySQL 由 Docker 容器提供，启动方式见 shell/dev-start.sh

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fitcoach?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: fitcoach
    password: fitcoach123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

logging:
  level:
    com.lanprojects.fitcoach: DEBUG
    org.hibernate.SQL: DEBUG
